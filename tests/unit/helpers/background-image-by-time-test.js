
import { backgroundImageByTime } from 'weather-app/helpers/background-image-by-time';
import { module, test } from 'qunit';

module('Unit | Helper | background image by time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = backgroundImageByTime([42]);
  assert.ok(result);
});

