
import { convertDegreesToCardinalFriendly } from 'weather-app/helpers/convert-degrees-to-cardinal-friendly';
import { module, test } from 'qunit';

module('Unit | Helper | convert degrees to cardinal friendly');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = convertDegreesToCardinalFriendly([42]);
  assert.ok(result);
});

