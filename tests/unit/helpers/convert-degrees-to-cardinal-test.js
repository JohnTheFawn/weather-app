
import { convertDegreesToCardinal } from 'weather-app/helpers/convert-degrees-to-cardinal';
import { module, test } from 'qunit';

module('Unit | Helper | convert degrees to cardinal');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = convertDegreesToCardinal([42]);
  assert.ok(result);
});

