
import { formatDateDayEnding } from 'weather-app/helpers/format-date-day-ending';
import { module, test } from 'qunit';

module('Unit | Helper | format date day ending');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatDateDayEnding([42]);
  assert.ok(result);
});

