
import { formatDateDayOfWeek } from 'weather-app/helpers/format-date-day-of-week';
import { module, test } from 'qunit';

module('Unit | Helper | format date day of week');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatDateDayOfWeek([42]);
  assert.ok(result);
});

