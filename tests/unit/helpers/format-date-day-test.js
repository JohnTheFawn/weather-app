
import { formatDateDay } from 'weather-app/helpers/format-date-day';
import { module, test } from 'qunit';

module('Unit | Helper | format date day');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatDateDay([42]);
  assert.ok(result);
});

