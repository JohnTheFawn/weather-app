
import { formatDateMonthNumber } from 'weather-app/helpers/format-date-month-number';
import { module, test } from 'qunit';

module('Unit | Helper | format date month number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatDateMonthNumber([42]);
  assert.ok(result);
});

