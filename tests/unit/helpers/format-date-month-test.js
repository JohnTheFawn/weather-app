
import { formatDateMonth } from 'weather-app/helpers/format-date-month';
import { module, test } from 'qunit';

module('Unit | Helper | format date month');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatDateMonth([42]);
  assert.ok(result);
});

