
import { formatTimeSuffix } from 'weather-app/helpers/format-time-suffix';
import { module, test } from 'qunit';

module('Unit | Helper | format time suffix');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatTimeSuffix([42]);
  assert.ok(result);
});

