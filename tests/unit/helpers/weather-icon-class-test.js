
import { weatherIconClass } from 'weather-app/helpers/weather-icon-class';
import { module, test } from 'qunit';

module('Unit | Helper | weather icon class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = weatherIconClass([42]);
  assert.ok(result);
});

